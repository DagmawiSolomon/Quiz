const container = document.querySelector(".questions-container")
const slide =document.querySelectorAll(".question")

// buttons 
const next = document.getElementById("next__btn")

//counter
let counter = 1
const size = slide[0].clientWidth;

